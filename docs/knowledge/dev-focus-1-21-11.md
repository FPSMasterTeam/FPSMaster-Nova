# Nova 当前开发版本重点

Nova 当前主要开发与验证目标是 Minecraft 1.21.11。涉及渲染、WebView、mixin、GPU API 或版本条件代码时，先在该版本完成验证，再检查 Stonecutter 生成的其他版本变体。

1.21.5 及以上使用新的渲染路径，不能直接套用旧版本 GL 行为。构建成功不等于运行正确；渲染相关改动还需启动、打开 WebView、调整窗口及重复进入界面进行冒烟验证。
