# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
# Fabric Properties
# check these on https://modmuss50.me/fabric.html
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.4
loader_version=0.19.3
kotlin_loader_version=1.13.12+kotlin.2.4.0
# Mod Properties — semver product line (shared scheme with Edge).
# CI publishes:
#   beta    -> {core}-beta.{GITHUB_RUN_NUMBER}  e.g. 1.0.0-beta.12
#   release -> {core}                          e.g. 1.0.0
# Modrinth per-MC: {version}+{minecraft}       e.g. 1.0.0-beta.12+1.21.1
mod_version=1.0.0-beta
maven_group=top.fpsmaster
archives_base_name=nova
